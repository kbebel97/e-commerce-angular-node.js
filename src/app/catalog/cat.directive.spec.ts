import { CatDirective } from './cat.directive';

describe('CatDirective', () => {
  it('should create an instance', () => {
    const directive = new CatDirective();
    expect(directive).toBeTruthy();
  });
});
